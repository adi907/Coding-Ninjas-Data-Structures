#include<bits/stdc++.h>
using namespace std;

bool isSafe(int** arr,int** solArr,int n,int x,int y){
	if(x>=n || x<0 || y>=n || y<0 || arr[x][y] ==0 || solArr[x][y] ==1){
		return false;
	}
	return true;
}

void ratMaze(int** maze,int n,int** solArr,int x,int y){
	if(x == n-1 && y == n-1){//base case:print and backtrack and return
		solArr[x][y] =1;
		for(int i=0;i<n;i++){
			for(int j=0;j<n;j++){
				cout << solArr[i][j] << " ";
			}
		}cout<<endl;
      	solArr[x][y]=0;
	return;
	}

	if(!isSafe(maze,solArr,n,x,y)){
		return;
	}

	solArr[x][y] = 1;
	ratMaze(maze,n,solArr,x-1,y);
	ratMaze(maze,n,solArr,x+1,y);
	ratMaze(maze,n,solArr,x,y-1);
	ratMaze(maze,n,solArr,x,y+1);
	solArr[x][y] = 0;
}

int main(){
	int n;
	cin>>n;

	int** arr=new int*[n];
	int** solArr=new int*[n];
	for(int i=0;i<n;i++){
		arr[i]=new int[n];
		solArr[i]=new int[n];
	}
	for(int i=0;i<n;i++){
		for(int j=0;j<n;j++){
			cin>>arr[i][j];
			solArr[i][j]=0;
		}
	}
	ratMaze(arr,n,solArr,0,0);
}
