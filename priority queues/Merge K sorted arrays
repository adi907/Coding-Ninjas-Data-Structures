 Given k different arrays, which are sorted individually (in ascending order).
 You need to merge all the given arrays such that output array should be sorted (in ascending order).
 
#include<bits/stdc++.h>
using namespace std;

void kSortedArray(int input[],int n,int k){
    priority_queue<int> pq;
    for(int i=0;i<k;i++){
        pq.push(input[i]);
    }
    
    int j=0;
    for(int i=k;i<n;i++){
        input[j]=pq.top();
        pq.pop();
        j++;
        pq.push(input[i]);
    }//till here sorts (n-k) elements as this loop starts from k & terminates @n

    while(!pq.empty()){//for remaining elements
        input[j]=pq.top();
        pq.pop();
        j++;
    }
}
